import React from 'react';

const Core = (props) => {
  return (
    <div className='logo-wrapper' style={props.styles}>
      <b className='icon icon-politico' />
    </div>
  );
};

export default Core;
